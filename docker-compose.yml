services:
  mysql:
    image: mysql:8.0
    container_name: dev-chat-mysql
    restart: unless-stopped
    env_file:
      - /home/ubuntu/Devchat/.env
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=devchat
      - MYSQL_USER=${MYSQL_USERNAME}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
    volumes:
      - ./mysql-data:/var/lib/mysql
    networks:
      - dev-chat-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-u", "root", "-p${MYSQL_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

  dev-chat-frontend:
    image: limkanghyun/dev-chat-frontend:latest
    container_name: dev-chat-frontend
    networks:
      - dev-chat-network

  dev-chat-backend-blue:
    image: limkanghyun/dev-chat-backend:latest
    container_name: dev-chat-backend-blue
    env_file:
      - /home/ubuntu/Devchat/.env
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - MYSQL_USERNAME=${MYSQL_USERNAME}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - RDS_ENDPOINT=${RDS_ENDPOINT}
    networks:
      - dev-chat-network
      - redis-sentinel_redis-net
    depends_on:
      mysql:
        condition: service_healthy

  dev-chat-backend-green:
    image: limkanghyun/dev-chat-backend:latest
    container_name: dev-chat-backend-green
    env_file:
      - /home/ubuntu/Devchat/.env
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - MYSQL_USERNAME=${MYSQL_USERNAME}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - RDS_ENDPOINT=${RDS_ENDPOINT}
    networks:
      - dev-chat-network
      - redis-sentinel_redis-net
    depends_on:
      mysql:
        condition: service_healthy

  nginx_proxy:
    image: nginx:latest
    container_name: nginx_proxy
    volumes:
      - ./deployment/nginx_proxy/nginx.conf:/etc/nginx/nginx.conf
      - /etc/letsencrypt:/etc/letsencrypt
      - ./www/certbot:/var/www/certbot
    ports:
      - "80:80"
      - "443:443"
    networks:
      - dev-chat-network
      
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus-data:/prometheus
    networks:
      - dev-chat-network

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    ports:
      - "3002:3000"
    volumes:
      - ./grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - dev-chat-network

networks:
  dev-chat-network:
    driver: bridge
    name: dev-chat-network
  redis-sentinel_redis-net:
    external: true
