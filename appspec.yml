version: 0.0
os: linux
files:
  - source: /deployment
    destination: /home/ubuntu/Devchat/deployment
    overwrite: yes
  - source: /scripts
    destination: /home/ubuntu/Devchat/scripts
    overwrite: yes
  - source: /docker-compose.yml
    destination: /home/ubuntu/Devchat
    overwrite: yes

permissions:
  - object: /home/ubuntu/Devchat/deployment
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755

hooks:
  AfterInstall:
    - location: scripts/deploy_hook.sh
      timeout: 300
      runas: ubuntu
